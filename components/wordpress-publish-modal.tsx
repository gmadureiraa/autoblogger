/**
 * @deprecated
 *
 * Esse arquivo foi substituído pelo modal genérico em
 * `components/publish-modal.tsx`, que cobre WordPress + Wix + Ghost.
 *
 * Mantemos esse stub só pra não quebrar imports antigos. Use:
 *
 *   import { PublishModal } from "@/components/publish-modal"
 *
 * Pode remover esse arquivo na próxima limpeza — não tem mais nenhum
 * `import` apontando pra ele dentro do app.
 */

export { PublishModal as WordPressPublishModal } from "./publish-modal"
